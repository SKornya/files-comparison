// const isObjectAndNotArray = (file, key) => typeof file[key]
//   === 'object' && !Array.isArray(file[key]);

// const getPlainDiff = (file1, file2, keys) => {
// // plug
// };

// export default getPlainDiff;
